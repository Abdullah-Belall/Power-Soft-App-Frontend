export enum PrivateStatusEnum {
  NORMAL = "normal",
  PRIVATE = "private",
}

export enum OrderStatusEnum {
  PENDING = "pending",
  IN_PROGRESS = "in_progress",
  COMPLETED = "completed",
  CANCELLED = "cancelled",
}
